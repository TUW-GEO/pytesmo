[build-system]
requires = ["numpy >= 1.7.0", "Cython >= 0.29.23", "setuptools>=46.1.0", "wheel", "setuptools_scm[toml]>=5"]
build-backend = "setuptools.build_meta"
[tool]
[tool.setuptools_scm]
version_scheme = "no-guess-dev"
